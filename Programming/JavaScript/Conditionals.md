
```
if (true) {
	do code
} else if (true) {
	do other code
} else {
	do even other code
}
```

```
switch(5)
	case condition:
		do code
		break;
	default:
		do code if no cases good
	
```

Ternary operator:

```
true ? do code : do other code; // the : is like an else
```